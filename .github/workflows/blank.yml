name: Dummy test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Runs a set of commands using the runners shell
      - name: Test GHA expressions language
        run: |
          echo null: ${{ null }}

          echo bools
          echo true: ${{ true }}
          echo false: ${{ false }}

          echo numbers
          echo 0: ${{ 0 }}
          echo 1: ${{ 1 }}
          echo 10: ${{ 10 }}
          echo -0: ${{ -0 }}
          echo -1: ${{ -1 }}

          echo strings

          echo empty string: ${{ '' }}
          echo \'potato\': ${{ 'potato' }}

          # will fail, this requires an expression
          # echo empty substitution: ${{  }}
          echo no array or object literals!





